name: coderspace_network
description: "A lightweight and developer-friendly Dio-based HTTP client for Flutter with clean response handling and model parsing."
version: 1.0.1
homepage: https://github.com/coderspacedev
repository: https://github.com/coderspacedev/coderspace_network
issue_tracker: https://github.com/coderspacedev/coderspace_network/issues
documentation: https://github.com/coderspacedev/coderspace_network#readme

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  mockito: ^5.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true